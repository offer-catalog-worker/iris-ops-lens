services:
  iris:
    build:
      context: .
      args:
        TESTS: 0
    command: --check-caps false
    ports:
      - "52773:52773"
    volumes:
      - ./web:/opt/irisops/csp
      - ./:/irisdev/app
